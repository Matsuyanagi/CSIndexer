model = "gpt-5.6-sol"
model_reasoning_effort = "max"

[features]
multi_agent = true

[agents]
enabled = true
max_concurrent_threads_per_session = 4

# Safe default when a spawn does not specify a model.
default_subagent_model = "gpt-5.6-terra"
default_subagent_reasoning_effort = "max"
